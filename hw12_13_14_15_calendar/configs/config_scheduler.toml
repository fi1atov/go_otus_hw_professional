duration = "120s"

[logger]
level = "DEBUG"
out_paths = ["stdout"]
err_paths = ["stderr"]

[database]
inmemory=false
connect="postgresql://postgres:postgres@localhost:6543/postgres?search_path=calendar"

[rmq]
host = "localhost"
port = "5672"
protocol = "amqp"
username = "guest"
password = "guest"
exchange = "test-exchange"
queue = "test-queue"
